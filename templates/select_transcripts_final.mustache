USE TRANSCRIPTS;
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;



SELECT
  TRANSCRIPT_ID,
  CONSULTATION_PARTICIPANT_ID as cpid,
  CONTENT,
  CREATE_DATE_UTC
FROM TRANSCRIPTS
WHERE 
  CONSULTATION_PARTICIPANT_ID IN (-1{{#cpids}},'{{.}}'{{/cpids}})
ORDER BY CREATE_DATE_UTC DESC
